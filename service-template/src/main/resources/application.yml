server:
  port: 8080

spring:
  application:
    name: service-template
management:
  endpoints:
    web:
      exposure:
        include: health, info

logging:
  level:
    root: info
    com.mario.alba: DEBUG
